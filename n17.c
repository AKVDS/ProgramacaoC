#include <stdio.h>
int main (){
    int i = 1;

    while (i <= 5)
    {
        printf ("%d\n", i);
        i++; // ++ vai pegar o valor de i e vai somar até chegar no resultado que foi pedido
    }
return 0;
}

